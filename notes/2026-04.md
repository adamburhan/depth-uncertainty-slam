# April 2026

## 2026-04-26

### Today's focus
Cluster setup: get pyslam installable and runnable on Mila. Need to think about whether to use conda, pixi, uv, singularity and if it may be better to use one of our lab's workstations.

### Done
- found rosdocker repo where pyslam and pyslam_cuda dockerfiles available
- built the images and installed them in snoopy
- tested soulcity/Easy/P001 and P000, takes a while to succeed initialization, tracking lost and fails to relocalize
- realized tartanair difficulty is bimodal - sequences either succeed at ceiling (sub decimeter) or fail completely 

### Open questions / blockers
- unable to do docker --gpus all, missing something (need to ping Kumar)
- Is bimodal ok? Tentative yes: easy = "method doesn't hurt" check, hard = "method helps" check (success rate / time-to-failure)

### Sanity-check status (carried forward from prior week)

| Test | Result | Interpretation |
|---|---|---|
| Vanilla mono on abandonedfactory P000 | 0.092 ± 0.026m (5 runs) | clean baseline |
| Ones-weighting (flag on, all-1) | 0.096 ± 0.036m (5 runs) | matches vanilla → wiring is no-op as expected |
| Random weights | 0.07–0.36m, bimodal (4 runs) | wiring fires occasionally but masked by Huber + redundancy |
| Anti-octave 1.2^(4·octave) | 0.095m (1 run) | no clear signal — sequence too easy |

Wiring verified live; abandonedfactory/Easy too forgiving for perturbation to consistently degrade.

### Next session
- The one part not working now is being able to reuse venv across sessions (when re running docker), need to fix this issue
- Build `scripts/aggregate_results.py` once cluster install completes (CSV schema: `[exp_name, config, sequence, run_idx, ate_rmse, rpe_rmse, success, n_lost_frames, git_hash, seed, timestamp]`)
- Phase 1 characterization sweep: vanilla mono on abandonedfactory/Easy/{P000..P005} + harder TartanAir scenes (westerndesert, endofworld, seasonsforest, oldtown). Goal: find sequences with vanilla ATE ∈ [0.3, 2m] or non-trivial failure rate
- Add `aux_uncertainty` plumbing to Frame / Tracking / Slam (~10 lines, future-proofs for covariance model)
- Write `notes/decisions.md` once cluster install lands as a coherent decision


## 2026-04-27

### Today's focus
- Think about exactly how multiple sequences will be run, sequential bash script is simplest even if long
- Fix the issue of needing to recreate venv each time I want to run pyslam
- Decide on the exact metrics to report per sequence run besides ate rmse and already reported stats (things like failure rate and other robustness metrics important)
- Once established, add the necessary logging to pyslam
- add missing code to be compatible with the outer bash script (how to pass configs and the sequences to run)
- write the run script, 5 runs each configuration (vanilla, bad weighting scheme)

## 2026-04-28

### Today's focus
Get the iteration loop solid and launch the Phase 1 sweep.

### Plan (re-ordered from initial draft)
1. **Fix venv persistence across container runs. Also, ensure codebase flexibility without massive rebuild and reinstall**
2. **Lock in per-run metrics.** Add to logging:
   - `frac_frames_ok`, `frac_frames_lost` — continuous robustness, escapes bimodality
   - `n_relocalization_events`, `longest_ok_streak` — trajectory stability
   - `ate_rmse_conditional` (computed only when `frac_frames_ok > 0.8`)
   - `success` boolean for coarse tables
3. **Outer bash runner.** One JSON per `(config, sequence, run_idx)`, runner skips if output exists → resumable. `aggregate_results.py` globs at end into the CSV schema from 04-26.
4. **Launch.** Vanilla mono + ones-weighting + random-weighting × 5 runs × Phase 1 sequence list.

### Open questions / blockers
- **Image slim is deferred, not abandoned.** ~152 GB on disk (63 GB content), 1.3 TB free → not blocking. Worth a half-day later to drop semantic stack (detectron2/detic/eov_seg/odise/rf_detr — none used), TensorFlow + DELF (unused), and all-but-one depth estimator (keep `depth_anything_v2`, drop ml_depth_pro / dav3 / raft_stereo / crestereo×2 / mast3r / mvdust3r / vggt×2 / fast3r). Estimated savings: ~20 GB content, much more on disk.

### Done (so far)
- Reclaimed ~160 GB on snoopy via `docker builder prune -a` + `docker volume prune` (build cache had grown to 152 GB during iteration).
- Got sudo access on snoopy
- downloaded eth3d (potentially more interesting sequences)
- got it to run on pyslam

### Risks to watch
- **Cherry-picking risk in sequence selection.** Phase 1 list (`{P000..P005}` + westerndesert/endofworld/seasonsforest/oldtown) should be locked *before* seeing per-method results. Report all sequences in final tables, including ones where method has no effect.
- Might need to download more of tartanair's sequences

### Next session
- Implement `_compute_kps_depth_weight(aux_depth)` patch-variance version.


## 2026-04-29

I'm currently running vanilla orbslam on all the sequences from eth3d. What am i looking for? Sequences where performance is not too easy nor catastrophic. 
