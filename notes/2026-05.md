# May 2026

## 2024-05-04

Big update today. I collected the results from running the depth derived heuristic uncertainty weighting based on ground truth rgbd depth on all sequences of ETH3D.

Observations: 
- Massive and visible improvements on table\_7, plant\_5 and cables\_1
- Generally keeps the same performance as baseline on successful sequences (no harm)
- Potentially more positive results: Since rgbd depth information is imperfect, I used a hack for modelling depth discontinuity variance, this needs to be improved since some sequences are a lot noisier in the recorded depth due to things like lighting, etc.

Next steps:
- Fix the heuristic depth approach
- Add tum-rgbd structure vs texture sequences to the eval
- Get depth estimator up and running, test it
- Integrate depth estimator to pyslam
- Add depth-anything depth estimation and apply same uncertainty weighting scheme
- Will need to rebuild the docker image to run on snoopy

Important note:
- currently I'm running pyslam on snoopy and using tmux, process is as follows:
    - `tmux new -s slam\_run\_i`
    - `docker run -it --rm -v /home/adam/scratch/datasets:/home/$USER/datasets  pyslam`
    - `pip install faiss-cpu`
    - `cd pyslam && ./main_slam_evaluation.py -c /home/adam/datasets/eth3d/pyslam_configs/eval_eth3d_i.json -o /home/adam/datasets/eth3d/pyslam_outputs`